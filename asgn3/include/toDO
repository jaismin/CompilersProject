scoping
return types not checked
boolean , remember a==flase

calling void functions are only allowed as statements and not for others



Add a return statement if one does not exist
check return type
