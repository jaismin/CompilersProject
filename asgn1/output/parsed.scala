object TargetTest2 extends App { 	// KWRD_OBJECT IDENTIFIER KWRD_EXTNDS IDENTIFIER BLOCK_BEGIN 
  def loop(body: => Unit): LoopUnlessCond = 	// KWRD_DEF IDENTIFIER LPAREN IDENTIFIER COLON FUNTYPE IDENTIFIER RPAREN COLON IDENTIFIER ASSIGN 
    new LoopUnlessCond(body) 	// KWRD_NEW IDENTIFIER LPAREN IDENTIFIER RPAREN 
  protected class LoopUnlessCond(body: => Unit) { 	// KWRD_PROTECTED KWRD_CLASS IDENTIFIER LPAREN IDENTIFIER COLON FUNTYPE IDENTIFIER RPAREN BLOCK_BEGIN 
    def unless(cond: => Boolean) { 	// KWRD_DEF IDENTIFIER LPAREN IDENTIFIER COLON FUNTYPE IDENTIFIER RPAREN BLOCK_BEGIN 
      body 	// IDENTIFIER 
      if (!cond) unless(cond) 	// KWRD_IF LPAREN NOT IDENTIFIER RPAREN IDENTIFIER LPAREN IDENTIFIER RPAREN 
    } 	// BLOCK_END 
  } 	// BLOCK_END 
  var i = 10 	// KWRD_VAR IDENTIFIER ASSIGN INT_CONST 
  loop { 	// IDENTIFIER BLOCK_BEGIN 
    println("i = " + i) 	// IDENTIFIER LPAREN STRING_CONST PLUS IDENTIFIER RPAREN 
    i -= 1 	// IDENTIFIER MINUSEQ INT_CONST 
  } unless (i == 0) 	// BLOCK_END IDENTIFIER LPAREN IDENTIFIER EQUAL INT_CONST RPAREN 
} 	// BLOCK_END
